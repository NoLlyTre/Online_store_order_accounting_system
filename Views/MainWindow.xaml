<Window x:Class="OrderSystemEF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Система учета заказов" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Заказы">
                <MenuItem Header="Мои заказы" Click="MyOrdersMenuItem_Click"/>
                <MenuItem Header="Все заказы" Click="AllOrdersMenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="Товары">
                <MenuItem Header="Каталог товаров" Click="ProductsMenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="Выход" Click="LogoutMenuItem_Click"/>
        </Menu>

        <TabControl Grid.Row="1" Name="MainTabControl">
            <TabItem Header="Каталог товаров" Name="ProductsTab">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <DataGrid Grid.Row="1" Name="ProductsDataGrid" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True" 
                              SelectionMode="Single"
                              Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
                            <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="300"/>
                            <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat='{}{0:C}'}" Width="100"/>
                            <DataGridTextColumn Header="Количество" Binding="{Binding StockQuantity}" Width="100"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <Button Grid.Row="0" Name="CreateOrderButton" 
                            Content="Создать заказ" 
                            Click="CreateOrderButton_Click" 
                            HorizontalAlignment="Right" 
                            Width="150" Height="30"/>
                </Grid>
            </TabItem>

            <TabItem Header="Мои заказы" Name="OrdersTab">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Button Grid.Row="0" Name="DeleteOrderButton" 
                            Content="Удалить выбранный заказ" 
                            Click="DeleteOrderButton_Click" 
                            HorizontalAlignment="Right" 
                            Width="200" Height="30"
                            Margin="0,0,0,10"/>
                    
                    <DataGrid Grid.Row="1" Name="OrdersDataGrid" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True" 
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding OrderDate, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Width="150"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat='{}{0:C}'}" Width="100"/>
                            <DataGridTextColumn Header="Адрес доставки" Binding="{Binding ShippingAddress}" Width="200"/>
                            <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>

